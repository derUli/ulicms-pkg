<?php 
function uliticket_render(){
  $html = "Not implemented Yet";
  if(!is_logged_in()){
     ulicms_rediect(„admin/“);
  } else {
    // Liste der Tickets ausgeben
    // Falls Kunde eingeloggt ist
    // Alle Tickets des Kunden anzeigen
    // Falls Mitarbeiter eingeloggt ist, alle offenen Tickets aller Kunden anzeigen
}
   return $html;
}